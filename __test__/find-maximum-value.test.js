'use strict';

const findMaxValue = require ('../find-maximum-value');

//create a binary tree

describe('find-Maximum-Value test searching for the maximum value', () => {
  test('should return 10', () =>
    expect(findMaxValue).toEqual(max value))
