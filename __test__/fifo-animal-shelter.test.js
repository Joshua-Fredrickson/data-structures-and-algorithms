'use strict';

describe('Test will take in a string of cat', () => {
  test('testing for cat output', () => {
    expect('cat').toEqual({ name: 'kitty', species: 'cat'});
  });
  test('testing for empty input')
});