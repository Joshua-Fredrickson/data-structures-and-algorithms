'use strict';

const LinkedList = require ('../ll-insertions');
const Nd = require('./nd')

describe('Linked Lists Insertion tests', () => {
   test('Linked List does not exist', () =>
    expect(testList).toEqual(undefined));

  // new Linkedlist(testList);


});
