'use strict';

const bFT = require ('../breadthFirstTraversal');

describe('Using breadthFirstTraversal output a string of the binary tree', () => {
  test('should return null', () =>
    expect(bFT()).toEqual(null))
});