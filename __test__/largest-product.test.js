'use strict';

const largestProduct = require('../largest-product.js');

describe('Find the largest product from the array' () => {
  test('Should return 5 from arrayZ[[1,1],[1,5],[1,1]]', () => {
    expect(largestProduct(arrayZ[[1,1],[1,5],[1,1]])).toEqual(5);
  });
});
