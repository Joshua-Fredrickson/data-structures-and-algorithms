'use strict';

const LinkedList = require ('../ll-insertions');


function kthFromEnd(k) {
  z = 0;
  const currNode = new Nd(newValue);
  let currNode = this.head;

  while(currNode.next !== null) {
    let listCounter = 0;
    currNode = currNode.next;
    z = listCounter;
  }
  let x = listCounter - k;

  while(x !== z) {
    let listCounter = 0;
    listCounter = listCounter + 1;
    currNode = currNode.next;
  }
  return currNode;
}
